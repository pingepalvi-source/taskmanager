# MongoDB URI - Replace with your MongoDB Atlas URI when deploying
# For local development with local MongoDB:
# spring.data.mongodb.uri=mongodb://localhost:27017/taskdb

# For MongoDB Atlas (production), set this environment variable on Render:
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://PP:<db_password>@cluster0.dw0oxwq.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=taskdb

server.port=8080
